{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认权限 — 允许基本的窗口和系统操作",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "clipboard-manager:default",
    "dialog:default",
    "fs:default"
  ]
}
